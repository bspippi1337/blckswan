# Install v1.5

Download `BLCKSWAN-v1.5-STORYMODE.apk` from the app Pages directory once the binary publish commit lands.

The build uses the same development signing certificate as v1.4 GAMELOOP, so Android can treat v1.5 as an update of that build. The manifest reports versionName `1.5`.

No network permission is added.
