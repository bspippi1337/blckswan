name: forge-test

on:
  push:

jobs:
  test:
    runs-on: self-hosted

    steps:
      - uses: actions/checkout@v4

      - run: |
          echo "FORGE ONLINE"
          hostname
          uptime
          docker ps
